<?xml version="1.0" encoding="UTF-8"?>
<!--
  Generated by WaterRock ant-to-maven-scaffold (per-module).
  Module: petstore  Directory: ws/apps/petstore

  Review before committing:
    - groupId: inferred from Java package — verify against your org
    - version: 1.0-SNAPSHOT
    - packaging: war
    - Add <parent> block pointing to pom-parent.xml coordinates once that
      file has been reviewed and renamed to pom.xml.
    - Copy <dependencies> from the root pom.xml (scaffold-pom-xml) that
      are relevant to this module.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--
    TODO: add <parent> block once pom-parent.xml is finalised:
    <parent>
      <groupId>com.example</groupId>
      <artifactId>parent</artifactId>
      <version>1.0-SNAPSHOT</version>
    </parent>
  -->

  <groupId>com.sun.javaee</groupId>
  <artifactId>petstore</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>war</packaging>

  <name>petstore</name>

  <properties>
    <maven.compiler.source>1.5</maven.compiler.source>
    <maven.compiler.target>1.5</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <!--
      Dependencies scanned from this module's lib/ directory.
      Known jars are mapped to Maven Central coordinates.
      UNKNOWN entries use groupId=local with an install:install-file hint —
      run the mvn command shown, then replace the block with real coordinates.
      Do NOT delete lib/ until after `mvn install` succeeds.
      Same-repo sibling modules (built in this reactor) should be declared
      as standard <dependency> entries pointing to their Maven coordinates
      rather than using install:install-file.
    -->
    <dependency>
      <groupId>net.sourceforge.nekohtml</groupId>
      <artifactId>nekohtml</artifactId>
      <version>1.9.22</version>
    </dependency>

    <dependency>
      <groupId>httpunit</groupId>
      <artifactId>httpunit</artifactId>
      <version>1.7</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>xerces</groupId>
      <artifactId>xercesImpl</artifactId>
      <version>2.12.2</version>
    </dependency>

    <!-- Migrate to com.rometools:rome-fetcher:2.1.0 -->
    <dependency>
      <groupId>rome</groupId>
      <artifactId>rome-fetcher</artifactId>
      <version>0.7</version>
    </dependency>

    <!--
      UNKNOWN JAR: bp-ui-5.jar
      Not found in Maven Central coordinate map.
      Install locally before building:
        mvn install:install-file -Dfile=lib/bp-ui-5.jar \
          -DgroupId=local -DartifactId=bp-ui \
          -Dversion=5 -Dpackaging=jar
      Then search https://search.maven.org for the real coordinates
      and replace this block.
    -->
    <dependency>
      <groupId>local</groupId>
      <artifactId>bp-ui</artifactId>
      <version>5</version>
    </dependency>

    <!-- Migrate to org.jdom2:jdom2:2.0.6 (package rename org.jdom → org.jdom2) -->
    <dependency>
      <groupId>org.jdom</groupId>
      <artifactId>jdom</artifactId>
      <version>1.1.3</version>
    </dependency>

    <!--
      UNKNOWN JAR: installer-builder.jar
      Not found in Maven Central coordinate map.
      Install locally before building:
        mvn install:install-file -Dfile=lib/installer-builder.jar \
          -DgroupId=local -DartifactId=installer-builder \
          -Dversion=0.0.0 -Dpackaging=jar
      Then search https://search.maven.org for the real coordinates
      and replace this block.
    -->
    <dependency>
      <groupId>local</groupId>
      <artifactId>installer-builder</artifactId>
      <version>0.0.0</version>
    </dependency>

    <!--
      UNKNOWN JAR: installer.jar
      Not found in Maven Central coordinate map.
      Install locally before building:
        mvn install:install-file -Dfile=lib/installer.jar \
          -DgroupId=local -DartifactId=installer \
          -Dversion=0.0.0 -Dpackaging=jar
      Then search https://search.maven.org for the real coordinates
      and replace this block.
    -->
    <dependency>
      <groupId>local</groupId>
      <artifactId>installer</artifactId>
      <version>0.0.0</version>
    </dependency>

    <!--
      UNKNOWN JAR: sfx4j-1.0.jar
      Not found in Maven Central coordinate map.
      Install locally before building:
        mvn install:install-file -Dfile=lib/sfx4j-1.0.jar \
          -DgroupId=local -DartifactId=sfx4j \
          -Dversion=1.0 -Dpackaging=jar
      Then search https://search.maven.org for the real coordinates
      and replace this block.
    -->
    <dependency>
      <groupId>local</groupId>
      <artifactId>sfx4j</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- Upgrade to org.apache.commons:commons-fileupload2-jakarta:2.x for Jakarta EE 9+ -->
    <dependency>
      <groupId>commons-fileupload</groupId>
      <artifactId>commons-fileupload</artifactId>
      <version>1.1.1</version>
    </dependency>

    <!-- Migrate to com.rometools:rome:2.1.0 (package com.sun.syndication → com.rometools.rome) -->
    <dependency>
      <groupId>rome</groupId>
      <artifactId>rome</artifactId>
      <version>0.8</version>
    </dependency>

    <!-- BLOCKER: Apache Shale retired 2009 — replace with JSF 2.3+ <f:ajax> or JAX-RS -->
    <dependency>
      <groupId>org.apache.shale</groupId>
      <artifactId>shale-remoting</artifactId>
      <version>1.0.5</version>
    </dependency>

    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>1.2</version>
    </dependency>

    <!-- BLOCKER: Lucene 2.x API incompatible with 9.x — migrate incrementally: 2→3→4→5→6→7→8→9 -->
    <dependency>
      <groupId>org.apache.lucene</groupId>
      <artifactId>lucene-core</artifactId>
      <version>2.0.0</version>
    </dependency>

    <!-- Consider replacing with SLF4J + jcl-over-slf4j bridge -->
    <dependency>
      <groupId>commons-logging</groupId>
      <artifactId>commons-logging</artifactId>
      <version>1.1</version>
    </dependency>
  </dependencies>

  <build>
    <!--
      sourceDirectory: detected from Ant src.dir property chain.
      If this matches src/main/java (Maven default) you can remove this element.
      If the scaffold directory moves did not relocate sources into this module
      (Phase 1 moves apply at repo root only — see #122), move them manually:
        mv src/java src/main/java
      then remove this element.
    -->
    <sourceDirectory>src/java</sourceDirectory>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
    </plugins>
  </build>
</project>

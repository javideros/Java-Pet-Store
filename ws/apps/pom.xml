<?xml version="1.0" encoding="UTF-8"?>
<!--
  Generated by WaterRock ant-to-maven-scaffold (multi-module aggregator).
  Modules are listed in topological build order (dependency leaves first).

  Review before use:
    1. Verify the <modules> list and build order below.
    2. Each module's pom.xml has a TODO <parent> block — fill in
       the groupId / artifactId / version from this file once confirmed.

  Coordinates: groupId and version are inferred from the root build.xml.
  Adjust if they differ from your organisation's Maven conventions.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--
    groupId is inferred from the root Java package; adjust to match your
    organisation's Maven conventions.
    Set artifactId to something like "my-project-parent" so it doesn't
    clash with any module artifactId.
  -->
  <groupId>com.sun.javaee</groupId>
  <artifactId>my-project-parent</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>my-project aggregator</name>

  <modules>
    <module>petstore</module>
    <module>standalone-tests/catalogclient</module>
  </modules>
</project>

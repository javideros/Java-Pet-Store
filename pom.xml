<?xml version="1.0" encoding="UTF-8"?>
<!--
  Generated by WaterRock ant-to-maven-scaffold recipe.
  Source: Ant build.xml + lib/ jars mapped to Maven POM conventions.

  NOTE — for single-module projects (build.xml at the repo root):
    If directory restructuring fired, sources are now at Maven standard
    locations (src/main/java, src/main/webapp, etc.) — no <sourceDirectory>
    override needed.
    If sources are NOT at the repo root (e.g. ws/apps/petstore/src/java/),
    the move_directory transforms did NOT fire and no restructuring happened.
    In that case use the per-module pom.xml generated alongside this file
    and add <sourceDirectory> pointing to the actual source location.

  NOTE — for multi-module projects:
    This file is generated only for single-module projects (one build.xml,
    no <ant dir="..."> sub-modules). If sub-modules are present, the root
    pom.xml is the aggregator generated by scaffold-aggregator-pom, and each
    sub-module has its own pom.xml generated under its directory.

  Review before committing:
    - groupId: inferred from Java package — verify it matches your org
    - version: 1.0-SNAPSHOT
    - packaging: jar (detected from build.xml)
    - Remove maven-war-plugin block below if packaging is not 'war'
    - Dependencies marked UNKNOWN need Maven coordinates from https://search.maven.org
  After merging, install Renovate to keep dependencies current.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--
    groupId: derived from Java package declarations in src/main/java/.
    Adjust if your organisation uses a different Maven groupId convention.
  -->
  <groupId>com.sun.javaee</groupId>
  <!-- artifactId: from <project name="..."> in build.xml -->
  <artifactId>my-project</artifactId>
  <!-- version: from <property name="version" value="..."/> in build.xml -->
  <version>1.0-SNAPSHOT</version>
  <!-- packaging: from is.war.module / is.ejb.module property or <war>/<ear> task -->
  <packaging>jar</packaging>

  <name>my-project</name>
  <description></description>

  <properties>
    <!--
      Ant javac task: source="1.5" target="1.5"
      Raise to at least 11 before migrating to Jakarta EE 9+ namespaces.
    -->
    <maven.compiler.source>1.5</maven.compiler.source>
    <maven.compiler.target>1.5</maven.compiler.target>
    <!--
      Ant javac task: encoding="UTF-8"
    -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>

    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>servlet-api</artifactId>
      <version>2.5</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.faces</groupId>
      <artifactId>jsf-api</artifactId>
      <version>1.2</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.persistence</groupId>
      <artifactId>persistence-api</artifactId>
      <version>1.0</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.ejb</groupId>
      <artifactId>javax.ejb-api</artifactId>
      <version>3.0</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.el</groupId>
      <artifactId>el-api</artifactId>
      <version>2.1</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.transaction</groupId>
      <artifactId>javax.transaction-api</artifactId>
      <version>1.1</version>
      <scope>provided</scope>
    </dependency>






    <!-- Consider replacing with SLF4J + jcl-over-slf4j bridge -->
    <dependency>
      <groupId>commons-logging</groupId>
      <artifactId>commons-logging</artifactId>
      <version>1.1</version>
    </dependency>

    <dependency>
      <groupId>net.sourceforge.nekohtml</groupId>
      <artifactId>nekohtml</artifactId>
      <version>1.9.22</version>
    </dependency>

    <dependency>
      <groupId>xerces</groupId>
      <artifactId>xercesImpl</artifactId>
      <version>2.12.2</version>
    </dependency>

    <dependency>
      <groupId>httpunit</groupId>
      <artifactId>httpunit</artifactId>
      <version>1.7</version>
      <scope>test</scope>
    </dependency>

    <!-- Migrate to com.rometools:rome:2.1.0 (package com.sun.syndication → com.rometools.rome) -->
    <dependency>
      <groupId>rome</groupId>
      <artifactId>rome</artifactId>
      <version>0.8</version>
    </dependency>

    <!-- Upgrade to org.apache.commons:commons-fileupload2-jakarta:2.x for Jakarta EE 9+ -->
    <dependency>
      <groupId>commons-fileupload</groupId>
      <artifactId>commons-fileupload</artifactId>
      <version>1.1.1</version>
    </dependency>

    <!-- Migrate to com.rometools:rome-fetcher:2.1.0 -->
    <dependency>
      <groupId>rome</groupId>
      <artifactId>rome-fetcher</artifactId>
      <version>0.7</version>
    </dependency>

    <!--
      UNKNOWN JAR: bp-ui-5.jar
      Not found in Maven Central coordinate map.
      Install locally before building:
        mvn install:install-file -Dfile=lib/bp-ui-5.jar \
          -DgroupId=local -DartifactId=bp-ui \
          -Dversion=5 -Dpackaging=jar
      Then search https://search.maven.org for the real coordinates
      and replace this block.
    -->
    <dependency>
      <groupId>local</groupId>
      <artifactId>bp-ui</artifactId>
      <version>5</version>
    </dependency>

    <!-- BLOCKER: Lucene 2.x API incompatible with 9.x — migrate incrementally: 2→3→4→5→6→7→8→9 -->
    <dependency>
      <groupId>org.apache.lucene</groupId>
      <artifactId>lucene-core</artifactId>
      <version>2.0.0</version>
    </dependency>

    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>1.2</version>
    </dependency>

    <!-- BLOCKER: Apache Shale retired 2009 — replace with JSF 2.3+ <f:ajax> or JAX-RS -->
    <dependency>
      <groupId>org.apache.shale</groupId>
      <artifactId>shale-remoting</artifactId>
      <version>1.0.5</version>
    </dependency>

    <!-- Migrate to org.jdom2:jdom2:2.0.6 (package rename org.jdom → org.jdom2) -->
    <dependency>
      <groupId>org.jdom</groupId>
      <artifactId>jdom</artifactId>
      <version>1.1.3</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>

      <!--
        WAR packaging: keep this plugin when packaging=war.
        Remove it for packaging=jar or packaging=ear.

        Maven WAR layout (after scaffold directory moves):
          src/main/java      → compiled to WEB-INF/classes/
          src/main/resources → classpath resources in WEB-INF/classes/
          src/main/webapp    → web application root (WEB-INF/, JSP, HTML, CSS, JS)

        Original Ant web dir: web
        The scaffold moves it to src/main/webapp (move-web-to-webapp transform).
        If the move did not fire, set warSourceDirectory to the original path.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.4.0</version>
        <configuration>
          <warSourceDirectory>src/main/webapp</warSourceDirectory>
        </configuration>
      </plugin>

      <!--
        Executable JAR: if a Main-Class was detected (),
        uncomment the block below and fill in the mainClass value.

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.4.2</version>
          <configuration>
            <archive>
              <manifest>
                <mainClass></mainClass>
              </manifest>
            </archive>
          </configuration>
        </plugin>
      -->

      <!--
        Distribution packaging: replaces Ant <zip> / assemble-dist targets
        that packaged the project into a downloadable ZIP or JAR bundle.

        Uncomment and configure if this project produces a distribution archive.
        Place an assembly descriptor at src/assembly/dist.xml listing the
        files and directories to include, then run: mvn package

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-assembly-plugin</artifactId>
          <version>3.7.1</version>
          <configuration>
            <descriptors>
              <descriptor>src/assembly/dist.xml</descriptor>
            </descriptors>
          </configuration>
          <executions>
            <execution>
              <id>make-assembly</id>
              <phase>package</phase>
              <goals><goal>single</goal></goals>
            </execution>
          </executions>
        </plugin>
      -->
    </plugins>
  </build>
</project>

<?xml version="1.0" encoding="UTF-8"?>
<!--
  Generated by WaterRock ant-to-maven-scaffold (multi-module aggregator).
  Modules are listed in topological build order (dependency leaves first).

  TO USE THIS FILE for a multi-module Maven project:
    1. Review the <modules> list below and verify the order is correct.
    2. Add a <groupId>, <artifactId>, <version>, and <properties> block
       (copy them from the sibling pom.xml generated by scaffold-pom-xml).
    3. Rename this file to pom.xml (deleting the existing single-module pom.xml).
    4. Each module's pom.xml (generated by scaffold-per-module-pom below)
       should declare this file's coordinates as its <parent>.

  For single-module projects: ignore this file; pom.xml is correct as-is.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- TODO: fill in from pom.xml or set your own coordinates -->
  <groupId>com.example</groupId>
  <artifactId>parent</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <modules>
    <module>ws/apps/petstore</module>
    <module>ws/apps/standalone-tests/catalogclient</module>
  </modules>
</project>
